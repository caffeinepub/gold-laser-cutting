{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Add hero tagline and place uploaded jewellery images into 6th gallery card",
  "requirements": [
    {
      "id": "REQ-14",
      "summary": "Add the exact tagline text to the homepage hero content area without changing existing heading/subheading wording.",
      "acceptanceCriteria": [
        "The hero section renders the tagline text exactly as: Your Trust, our Quality Work",
        "The existing hero H1 text (\"Best Gold Laser Cutting Service in Etawah\") and existing subheading text remain unchanged",
        "The tagline is visible on mobile and desktop and matches the existing black-and-gold premium styling"
      ],
      "file_operations": [
        {
          "path": "frontend/src/components/sections/HeroSection.tsx",
          "operation": "modify",
          "description": "Insert a new tagline line in the hero content area (keep the existing H1 and subheading text exactly as-is), styled to match the current black-and-gold theme and remain readable on mobile/desktop."
        }
      ]
    },
    {
      "id": "REQ-15",
      "summary": "Add the 3 uploaded images as static public assets and render them only inside the Gallery grid’s 6th card without impacting other cards.",
      "acceptanceCriteria": [
        "The following files are added to the frontend public assets and are served correctly: Casting-Jewellery-Ring.jpg, Casting_type_gold_pendant_necklace_with_white_stones_by_Mannil_Jewellers__20HzeFw.webp, casting-ladies-gold-rings.jpg",
        "The Gallery section’s 6th grid card displays all three uploaded images (e.g., as an in-card collage/slider) without affecting the other gallery cards",
        "All displayed images include non-empty alt text in English",
        "Images load without layout shift and preserve the existing gallery hover/animation style"
      ],
      "file_operations": [
        {
          "path": "frontend/public/assets/Casting-Jewellery-Ring.jpg",
          "operation": "create",
          "description": "Add the uploaded image as a static public asset so it is served by the frontend."
        },
        {
          "path": "frontend/public/assets/Casting_type_gold_pendant_necklace_with_white_stones_by_Mannil_Jewellers__20HzeFw.webp",
          "operation": "create",
          "description": "Add the uploaded image as a static public asset so it is served by the frontend."
        },
        {
          "path": "frontend/public/assets/casting-ladies-gold-rings.jpg",
          "operation": "create",
          "description": "Add the uploaded image as a static public asset so it is served by the frontend."
        },
        {
          "path": "frontend/src/components/sections/GallerySection.tsx",
          "operation": "modify",
          "description": "Update the Gallery grid rendering so only the 6th card (index 5) displays the three uploaded images (e.g., a fixed-aspect collage within the existing card container), while cards 1–5 continue to render exactly as before; ensure non-empty English alt text, avoid layout shift via the existing fixed aspect-ratio container, and preserve current hover/overlay animations."
        }
      ]
    }
  ]
}