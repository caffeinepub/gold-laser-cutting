# Specification

## Summary
**Goal:** Add a new hero tagline and include the 3 uploaded jewellery images in the Gallery section’s 6th card.

**Planned changes:**
- Add a new tagline line in the homepage hero content area with the exact text: "Your Trust, our Quality Work" (keep existing hero heading/subheading unchanged).
- Add the uploaded image files as static frontend assets and render all three images inside the Gallery grid’s 6th card (only), preserving existing hover/animation behavior and preventing layout shift.
- Ensure each displayed image has non-empty English alt text.

**User-visible outcome:** The homepage hero shows the additional tagline, and the Gallery’s 6th card displays the three uploaded jewellery images on both mobile and desktop.
